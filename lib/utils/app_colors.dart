import 'package:flutter/material.dart';

class AppColors{

  static const MAIN_COLOR = Color(0xFF27AE60);
  static BoxShadow boxShadow = BoxShadow(
    offset: Offset(0, 0),
    blurRadius: 8,
    spreadRadius: 2,
    color: Colors.grey.withOpacity(0.2),
  );



}